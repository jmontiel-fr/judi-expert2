# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
env/

# ── Node.js ──
node_modules/
.next/
out/

# ── Docker ──
*.tar
*.tar.gz

# ── Binaires ──
*.exe
*.msi
*.dmg

# ── Environment ──
.env.aws
.env.dev
.env.*.local
central-site/.env
# client-site/.env est généré par les scripts dev/package (copie de .env.local ou .env.prod)
client-site/.env

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── Tests ──
.pytest_cache/
.hypothesis/
.coverage
htmlcov/
.mypy_cache/

# ── Build artifacts ──
central-site/app_client_package/.staging/
central-site/app_client_package/output/*.exe
central-site/app_client_package/output/*.sh

# ── Data (local dev) ──
*.db
data/

# ── Terraform ──
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
secrets.auto.tfvars
nul
.env.publish
